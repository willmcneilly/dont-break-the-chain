<h1>Listing links</h1>

<table>
  <tr>
    <th>Complete</th>
    <th>Completedat</th>
    <th>Timespent</th>
    <th>Information</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @links.each do |link| %>
  <tr>
    <td><%= link.complete %></td>
    <td><%= link.completedat %></td>
    <td><%= link.timespent %></td>
    <td><%= link.information %></td>
    <td><%= link_to 'Show', link %></td>
    <td><%= link_to 'Edit', edit_link_path(link) %></td>
    <td><%= link_to 'Destroy', link, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Link', new_link_path %>
